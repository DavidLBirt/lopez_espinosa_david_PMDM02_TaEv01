import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lopez_espinosa_david_PMDM02_TaEv01',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
